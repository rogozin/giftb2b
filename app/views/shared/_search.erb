	        	  <% if controller_name == "search" && params[:request].blank? -%>
	        	    <%= render :partial => 'search/ext_search2', :locals => {:ext_search => current_user.is_firm_user? || current_user.is_admin?}  %>
	        	  <% else -%>
	        	    <%= render :partial => 'search/light_search' %>	        	  
	        	  <% end -%>

