<% selected ||=[] -%>
<ul>
 <% @categories.each do |item| -%>
    <li><%= check_box_tag "category_ids[]", item.id, selected && selected.compact.map(&:to_i).include?(item.id), :id => "cat_#{item.id}" %>
    <%= label_tag "cat_#{item.id}", item.name %></li>
 <% end -%>
</ul>
