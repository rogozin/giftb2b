<h1>Возврат образца от клиента "<%= @sample.lk_firm.name if @sample.lk_firm %>"</h1>

<p>
<% if @sample.client_return_date == Date.today -%>
  <h2 style="color:red">Внимание!</h2> Сегодня наступает срок возврата образца <strong>"<%= @sample.name %>"</strong>.  <br />

В случае невозврата образца он будет оприходован, а его стоимость 
(<span style="color:red"><%=  number_to_currency(@sample.buy_price, :unit => 'руб. ') %></span>) 
 будет <strong>взыскана</strong> с Вас!
<% else -%>  
  Напоминаем Вам, что через <%= time_ago_in_words(@sample.client_return_date)  %> истекает срок возврата образца "<%= @sample.name %>". <br />
  По истечению срока (<%= @sample.client_return_date %>), образец будет оприходован, а его возврат невозможен.
<% end -%>
</p>


<h3>Информация о поставщике:</h3>
<p>
  Название: "<%= @sample.supplier.name if @sample.supplier %>" <br />
  Дата возврата образца: <%= @sample.supplier_return_date %>
</p>
<p>
<%= link_to "Перейти на страницу образца", edit_lk_sample_url(@sample) %>
</p>
