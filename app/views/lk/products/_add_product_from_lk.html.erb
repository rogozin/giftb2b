<div class="spacer"></div>
<h3>Мои товары:</h3>
<div class="spacer"></div>
<div class="gray-block">
<%= form_tag lk_load_products_path(:id => params[:id], :object_type => @object_type ), :remote => true, :method => :get, :id => "filter" do %>
Поиск:
<span><%= text_field_tag :request, params[:request], :placeholder => "Название" %></span>
<span><%= link_to 'Выбрать категорию товара', "#", :id => "select_category", :class => "pseudo-link" %></span>
<span id="category_ids"></span>
<div id="categories_dialog" title="Выбор категории" class="g-hidden">
<%= min_categories_tree(@catalog, @category_ids, params[:category_ids] ? params[:category_ids].map(&:to_i) : []) %>
</div>
<span><%= submit_tag "Найти" %></span>
<% end %>
</div>
<%= form_tag post_url do -%>
<%= render :partial => 'products', :locals => {:products => @lk_products, :should_check => true} %>
<div class="b-submit"><%= submit_tag "Добавить" if @lk_products.present? %></div>
<% end -%>
<div class="spacer"></div>
<%= render :partial => 'shared/paginate2', :locals => {:products => @lk_products} %>

