<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false&amp;region=RU"></script>
  <div id="firms">
  <table width="100%">
   <tr>
      <th>Город</th>
     <th>Станция метро</th>
     <th>Компания</th>
     <th>Адрес</th>
      <%= content_tag :th if defined?(send_order) && send_order %>
   </tr>
  <% @firms.each do |item| -%>
      <tr>
        <td><%= item.city %></td>
        <td><%= item.subway %></td>
        <td><%= link_to item.short_name, firm_path(item.permalink), :class => "firm-dialog" %></td>
        <td><%= item.addr_f %></td>
        <%=content_tag :td,  submit_tag("Отправить заказ", :name => item.id) if defined?(send_order) && send_order  %></td>
      </tr>
  <% end -%>
  </table>
<div class="spacer"></div>               
<p class="b-info">
  В этом списке может быть Ваше агентство. Подробнее по тел. +7 (495) 741-06-95
</p>        
  </div>
  
<div id="firm_info" class="g-hidden">
  
</div>

